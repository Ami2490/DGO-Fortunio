const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // I don't have this

// Use the local firebase if possible, but I don't have admin access here easy.
// Actually, I can just tell Alan to re-upload.
